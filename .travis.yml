sudo: required

language: python

services:
  - docker

notifications:
  email:
    on_success: change
    on_failure: always

before_install:
  docker build -t openbabel:python2.7.15 .

script:
  # SMOKE TEST
  - docker run --rm -it openbabel:python2.7.15 python -c 'import openbabel'
